# Unsupervised Machine Learning: K-Means Regional Centroid Segmentation
import pandas as pd
from sklearn.cluster import KMeans
print("Executing Scikit-Learn KMeans clustering (k=3) on density variables...")
